{
  "task": "vector-index",
  "version": "1.0",
  "description": "Indexes a vector with its metadata into a vector store",
  "inputs": [
    {
      "name": "id",
      "datatype": "BYTES",
      "shape": [1],
      "description": "Unique document identifier",
      "required": true
    },
    {
      "name": "vector",
      "datatype": "FP32",
      "shape": [-1],
      "description": "Dense embedding vector to index",
      "required": true
    },
    {
      "name": "metadata",
      "datatype": "BYTES",
      "shape": [1],
      "description": "JSON-encoded dict of metadata fields to store alongside the vector",
      "required": false
    }
  ],
  "outputs": [
    {
      "name": "indexed",
      "datatype": "BOOL",
      "shape": [1],
      "description": "True when the vector was successfully written to the store"
    }
  ]
}
