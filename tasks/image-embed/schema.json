{
  "task": "image-embed",
  "version": "1.0",
  "description": "Encodes an image into a dense vector embedding (e.g. CLIP)",
  "inputs": [
    {
      "name": "image_url",
      "datatype": "BYTES",
      "shape": [1],
      "description": "URL of the image to embed",
      "required": true
    }
  ],
  "outputs": [
    {
      "name": "embedding",
      "datatype": "FP32",
      "shape": [-1],
      "description": "Dense embedding vector (dimension depends on the model)"
    }
  ]
}
