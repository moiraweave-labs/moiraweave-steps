{
  "task": "text-embed",
  "version": "1.0",
  "description": "Encodes text into a dense vector embedding",
  "inputs": [
    {
      "name": "text",
      "datatype": "BYTES",
      "shape": [1],
      "description": "Input text to embed",
      "required": true
    }
  ],
  "outputs": [
    {
      "name": "embedding",
      "datatype": "FP32",
      "shape": [-1],
      "description": "Dense embedding vector (dimension depends on the model)"
    }
  ]
}
